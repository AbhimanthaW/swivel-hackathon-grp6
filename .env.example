# Browser -> local frontend dev proxy -> your Python backend.
# This is an origin, without /api/v1. The proxy preserves the route.
BACKEND_ORIGIN=http://127.0.0.1:8001
PORT=5173
# LLM keys belong ONLY in the future backend environment, never in frontend files.
